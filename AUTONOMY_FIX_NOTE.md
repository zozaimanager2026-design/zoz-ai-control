# Autonomy check policy

The autonomy workflow must distinguish core health from external connector setup. Missing WhatsApp credentials is an external setup blocker, not proof that the safe autonomous core is unhealthy.

Financial approval and verified-connector safety boundaries remain mandatory.