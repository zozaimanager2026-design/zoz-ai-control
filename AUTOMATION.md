# ZOZ AI — Automation Readiness

## الهدف

هذا الملف يحدد ما يمكن لـ ZOZ AI تنفيذه تلقائيًا وما يحتاج اعتمادًا خارجيًا أو قرارًا من المستخدم.

## الحالة الحالية

- Core API: جاهز.
- GitHub repository: متصل.
- Vercel deployment: موجود ومتحقق عبر GitHub status.
- Safe execution: مفعّل للمهام الداخلية غير المالية.
- Financial approval gate: إلزامي.
- Persistent storage: يحتاج `KV_REST_API_URL` و`KV_REST_API_TOKEN` في Production.
- WhatsApp / YouTube / TikTok / LinkedIn / Shopify: تحتاج اعتمادًا فعليًا قبل أي تنفيذ خارجي.

## قاعدة التنفيذ

1. نفّذ تلقائيًا كل ما لا يحتاج سرًا خارجيًا ولا قرارًا ماليًا.
2. لا تُنشئ بيانات اعتماد وهمية.
3. لا تُعتبر الخدمة متصلة إلا بعد توفر اعتماد فعلي والتحقق من الاتصال.
4. الدفع والشراء والتحويل والسحب واستلام الأموال تتطلب موافقة المستخدم.
5. لا يتم إنشاء مشروع Vercel جديد إذا كان المشروع الحالي موجودًا.

## مسار الانتقال

1. التخزين الدائم: يضاف فقط بعد توفير متغيرات البيئة المطلوبة.
2. WhatsApp: اعتماد الحساب ثم اختبار اتصال القراءة/الإرسال دون تنفيذ مالي.
3. YouTube: اعتماد الحساب ثم اختبار القراءة والنشر التجريبي.
4. TikTok: اعتماد الحساب ثم اختبار القراءة والنشر التجريبي.
5. LinkedIn: اعتماد الحساب ثم اختبار القراءة والنشر التجريبي.
6. Shopify: اعتماد المتجر ثم اختبار القراءة وإدارة غير مالية.
7. اختبار تكاملي شامل ثم تشغيل الاستقلالية الآمنة.

## ملاحظة

وجود اسم متغير بيئي في الكود لا يثبت أن الحساب متصل. التحقق الحقيقي يجب أن يحدث من خلال استدعاء الخدمة نفسها بعد توفر الاعتماد.
