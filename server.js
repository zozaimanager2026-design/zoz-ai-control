const crypto = require("crypto");
const express = require("express");
const dotenv = require("dotenv");
const path = require("path");
const business = require("./business");

dotenv.config();

const app = express();
const PORT = process.env.PORT || 3000;
const ZOZ_NAME = process.env.ZOZ_NAME || "ZOZ AI";
const WHATSAPP_API_VERSION = process.env.WHATSAPP_API_VERSION || "v23.0";

app.use(express.json({ verify: (req, res, buffer) => { req.rawBody = Buffer.from(buffer); } }));
app.use(express.static(path.join(__dirname, "public")));

const KV_REST_API_URL = process.env.KV_REST_API_URL || process.env.ZOZ_KV_REST_API_URL;
const KV_REST_API_TOKEN = process.env.KV_REST_API_TOKEN || process.env.ZOZ_KV_REST_API_TOKEN;
const DATABASE_URL = process.env.DATABASE_URL || process.env.ZOZ_DATABASE_URL;

const persistence = {
  enabled: Boolean((KV_REST_API_URL && KV_REST_API_TOKEN) || DATABASE_URL),
  provider: DATABASE_URL ? "postgresql" : ((KV_REST_API_URL && KV_REST_API_TOKEN) ? "KV-compatible REST" : "memory"),
  key: process.env.ZOZ_STATE_KEY || "zoz-ai:state"
};

const state = {
  name: ZOZ_NAME, status: "running", autonomy: true, financialApprovalRequired: true,
  jobs: [], approvals: [], audit: [], business: business.createBusinessState(),
  connectors: {
    github: { status: "connected", label: "GitHub", automation: "ready" },
    vercel: { status: "deployed", label: "Vercel", automation: "deployment_verified" },
    database: { status: persistence.enabled ? "configured_unverified" : "needs_setup", label: "Database", automation: "verification_required" },
    whatsapp: { status: process.env.WHATSAPP_ACCESS_TOKEN && process.env.WHATSAPP_PHONE_NUMBER_ID ? "configured_unverified" : "needs_setup", label: "WhatsApp", automation: "verification_required" },
    youtube: { status: process.env.YOUTUBE_ACCESS_TOKEN ? "configured_unverified" : "needs_setup", label: "YouTube", automation: "verification_required" },
    tiktok: { status: process.env.TIKTOK_ACCESS_TOKEN ? "configured_unverified" : "needs_setup", label: "TikTok", automation: "verification_required" },
    linkedin: { status: process.env.LINKEDIN_ACCESS_TOKEN ? "configured_unverified" : "needs_setup", label: "LinkedIn", automation: "verification_required" },
    shopify: { status: process.env.SHOPIFY_ACCESS_TOKEN ? "configured_unverified" : "needs_setup", label: "Shopify", automation: "verification_required" }
  }
};

function audit(event, details = {}) { state.audit.push({ id: `${Date.now()}-${state.audit.length}`, event, details, at: new Date().toISOString() }); if (state.audit.length > 500) state.audit.splice(0, state.audit.length - 500); }
async function fetchJson(url, options = {}) { const controller = new AbortController(); const timer = setTimeout(() => controller.abort(), 8000); try { const response = await fetch(url, { ...options, signal: controller.signal }); const text = await response.text(); let body = null; try { body = text ? JSON.parse(text) : null; } catch { body = { text: text.slice(0, 300) }; } return { ok: response.ok, status: response.status, body }; } finally { clearTimeout(timer); } }
async function kvGet(key) { if (!KV_REST_API_URL || !KV_REST_API_TOKEN) return null; const base = KV_REST_API_URL.replace(/\/$/, ""); const response = await fetchJson(`${base}/get/${encodeURIComponent(key)}`, { headers: { Authorization: `Bearer ${KV_REST_API_TOKEN}` } }); if (!response.ok) throw new Error(`Persistence GET failed: ${response.status}`); return response.body; }
async function kvSet(key, value) { if (!KV_REST_API_URL || !KV_REST_API_TOKEN) return null; const base = KV_REST_API_URL.replace(/\/$/, ""); const response = await fetchJson(`${base}/set/${encodeURIComponent(key)}`, { method: "POST", headers: { Authorization: `Bearer ${KV_REST_API_TOKEN}`, "Content-Type": "application/json" }, body: JSON.stringify(value) }); if (!response.ok) throw new Error(`Persistence SET failed: ${response.status}`); return response.body; }
async function loadState() { if (KV_REST_API_URL && KV_REST_API_TOKEN) { try { const result = await kvGet(persistence.key); if (result && result.result) { const saved = typeof result.result === "string" ? JSON.parse(result.result) : result.result; if (saved && typeof saved === "object") { state.jobs = Array.isArray(saved.jobs) ? saved.jobs : []; state.approvals = Array.isArray(saved.approvals) ? saved.approvals : []; state.audit = Array.isArray(saved.audit) ? saved.audit.slice(-500) : []; if (saved.business && typeof saved.business === "object") state.business = { ...business.createBusinessState(), ...saved.business }; } } } catch (error) { console.error("State load warning:", error.message); audit("persistence_load_failed", { message: error.message }); } } }
async function saveState() { if (KV_REST_API_URL && KV_REST_API_TOKEN) { try { await kvSet(persistence.key, { ...state, savedAt: new Date().toISOString() }); } catch (error) { console.error("State save warning:", error.message); } } }
function isFinanciallySensitive(text = "") { return /دفع|شراء|تحويل|سحب|استلام\s*أموال|استلام\s*اموال|بنك|بطاقة|bank|card|payment|purchase|transfer|withdraw/i.test(text); }
function isValidWhatsAppSignature(req) { const secret = process.env.WHATSAPP_APP_SECRET; if (!secret) return true; const signature = String(req.get("x-hub-signature-256") || ""); if (!signature.startsWith("sha256=") || !req.rawBody) return false; const expected = `sha256=${crypto.createHmac("sha256", secret).update(req.rawBody).digest("hex")}`; const actualBuffer = Buffer.from(signature); const expectedBuffer = Buffer.from(expected); return actualBuffer.length === expectedBuffer.length && crypto.timingSafeEqual(actualBuffer, expectedBuffer); }
function authorizeCron(req, res) { const secret = process.env.CRON_SECRET; if (!secret) { audit("autonomy_cycle_auth_failed", { reason: "cron_secret_missing", production: Boolean(process.env.VERCEL) }); return res.status(process.env.VERCEL ? 503 : 401).json({ ok: false, error: process.env.VERCEL ? "cron_secret_not_configured" : "cron_secret_required" }); } const expected = `Bearer ${secret}`; const actual = String(req.get("authorization") || ""); const actualBuffer = Buffer.from(actual); const expectedBuffer = Buffer.from(expected); if (actualBuffer.length !== expectedBuffer.length || !crypto.timingSafeEqual(actualBuffer, expectedBuffer)) { audit("autonomy_cycle_auth_failed", { reason: "invalid_cron_authorization" }); return res.status(401).json({ ok: false, error: "unauthorized" }); } return null; }

async function sendWhatsAppText(to, text) {
  if (!process.env.WHATSAPP_ACCESS_TOKEN || !process.env.WHATSAPP_PHONE_NUMBER_ID) return { ok: false, status: 503, error: "whatsapp_credentials_missing" };
  if (!to || !text) return { ok: false, status: 400, error: "recipient_and_text_required" };
  const url = `https://graph.facebook.com/${WHATSAPP_API_VERSION}/${encodeURIComponent(process.env.WHATSAPP_PHONE_NUMBER_ID)}/messages`;
  const response = await fetchJson(url, {
    method: "POST",
    headers: { Authorization: `Bearer ${process.env.WHATSAPP_ACCESS_TOKEN}`, "Content-Type": "application/json" },
    body: JSON.stringify({ messaging_product: "whatsapp", recipient_type: "individual", to: String(to), type: "text", text: { preview_url: false, body: String(text) } })
  });
  audit("whatsapp_outbound_attempt", { to: String(to).slice(0, 6) + "***", ok: response.ok, httpStatus: response.status });
  return { ok: response.ok, status: response.status, messageId: response.body?.messages?.[0]?.id || null, error: response.ok ? null : "whatsapp_api_rejected_request" };
}

async function verifyConnector(id) { try { let result; if (id === "database") { if (!persistence.enabled) return { id, status: "needs_setup", verified: false, reason: "Database/KV credentials missing" }; if (KV_REST_API_URL && KV_REST_API_TOKEN) { const data = await kvGet(persistence.key); result = { id, status: "verified", verified: true, detail: "KV REST read succeeded", hasState: Boolean(data && data.result) }; } else { result = { id, status: "configured_unverified", verified: false, detail: "DATABASE_URL configured; database adapter pending" }; } } else if (id === "whatsapp") { if (!process.env.WHATSAPP_ACCESS_TOKEN || !process.env.WHATSAPP_PHONE_NUMBER_ID) return { id, status: "needs_setup", verified: false, reason: "WHATSAPP_ACCESS_TOKEN or WHATSAPP_PHONE_NUMBER_ID missing" }; const r = await fetchJson(`https://graph.facebook.com/${WHATSAPP_API_VERSION}/${encodeURIComponent(process.env.WHATSAPP_PHONE_NUMBER_ID)}?fields=id,display_phone_number,verified_name`, { headers: { Authorization: `Bearer ${process.env.WHATSAPP_ACCESS_TOKEN}` } }); result = { id, status: r.ok ? "verified" : "error", verified: r.ok, httpStatus: r.status, detail: r.ok ? "WhatsApp phone number verified" : "WhatsApp API rejected the credential" }; } else if (id === "youtube") { if (!process.env.YOUTUBE_ACCESS_TOKEN) return { id, status: "needs_setup", verified: false, reason: "YOUTUBE_ACCESS_TOKEN missing" }; const r = await fetchJson("https://www.googleapis.com/youtube/v3/channels?part=id,snippet&mine=true", { headers: { Authorization: `Bearer ${process.env.YOUTUBE_ACCESS_TOKEN}` } }); result = { id, status: r.ok ? "verified" : "error", verified: r.ok, httpStatus: r.status, detail: r.ok ? "YouTube OAuth token accepted" : "YouTube API rejected the credential", channelCount: r.body?.pageInfo?.totalResults ?? null }; } else if (id === "tiktok") { if (!process.env.TIKTOK_ACCESS_TOKEN) return { id, status: "needs_setup", verified: false, reason: "TIKTOK_ACCESS_TOKEN missing" }; const r = await fetchJson("https://open.tiktokapis.com/v2/user/info/?fields=open_id,display_name", { headers: { Authorization: `Bearer ${process.env.TIKTOK_ACCESS_TOKEN}` } }); result = { id, status: r.ok ? "verified" : "error", verified: r.ok, httpStatus: r.status, detail: r.ok ? "TikTok OAuth token accepted" : "TikTok API rejected the credential" }; } else if (id === "linkedin") { if (!process.env.LINKEDIN_ACCESS_TOKEN) return { id, status: "needs_setup", verified: false, reason: "LINKEDIN_ACCESS_TOKEN missing" }; const r = await fetchJson("https://api.linkedin.com/v2/userinfo", { headers: { Authorization: `Bearer ${process.env.LINKEDIN_ACCESS_TOKEN}` } }); result = { id, status: r.ok ? "verified" : "error", verified: r.ok, httpStatus: r.status, detail: r.ok ? "LinkedIn OAuth token accepted" : "LinkedIn API rejected the credential" }; } else if (id === "shopify") { if (!process.env.SHOPIFY_ACCESS_TOKEN || !process.env.SHOPIFY_STORE_DOMAIN) return { id, status: "needs_setup", verified: false, reason: "SHOPIFY_ACCESS_TOKEN or SHOPIFY_STORE_DOMAIN missing" }; const domain = process.env.SHOPIFY_STORE_DOMAIN.replace(/^https?:\/\//, "").replace(/\/$/, ""); const r = await fetchJson(`https://${domain}/admin/api/2026-07/shop.json`, { headers: { "X-Shopify-Access-Token": process.env.SHOPIFY_ACCESS_TOKEN, Accept: "application/json" } }); result = { id, status: r.ok ? "verified" : "error", verified: r.ok, httpStatus: r.status, detail: r.ok ? "Shopify Admin API accepted the credential" : "Shopify API rejected the credential" }; } else if (id === "github") result = { id, status: "verified", verified: true, detail: "Connected through GitHub integration" }; else if (id === "vercel") result = { id, status: "verified", verified: true, detail: "Existing Vercel deployment is known" }; else return { id, status: "unknown", verified: false, reason: "Unsupported connector" }; state.connectors[id].status = result.status; state.connectors[id].automation = result.verified ? "ready" : "verification_failed"; audit("connector_verification", { id, status: result.status, verified: result.verified }); return result; } catch (error) { state.connectors[id].status = "error"; state.connectors[id].automation = "verification_failed"; audit("connector_verification_error", { id, message: error.message }); return { id, status: "error", verified: false, reason: error.name === "AbortError" ? "verification_timeout" : "verification_failed" }; } }
function readiness() { const blockers = []; for (const [id, connector] of Object.entries(state.connectors)) { if (id === "database" && connector.status === "needs_setup") continue; if (["needs_setup", "configured_unverified", "error"].includes(connector.status) && !["github", "vercel"].includes(id)) blockers.push({ id, status: connector.status, priority: id === "whatsapp" ? 2 : 3, owner: "user", action: connector.status === "configured_unverified" ? `تشغيل التحقق الفعلي لـ ${connector.label}` : `إكمال اعتماد ${connector.label}` }); } blockers.sort((a, b) => a.priority - b.priority); return { ok: blockers.length === 0, service: ZOZ_NAME, core: "healthy", persistence: persistence.enabled ? "enabled" : "memory_only", financialApprovalRequired: true, autonomy: true, blockers, business: business.businessSummary(state.business), approvalsPending: state.approvals.filter((item) => item.status === "pending").length }; }
function executionPlan() { const r = readiness(); return { service: ZOZ_NAME, mode: "ordered_execution", rule: "نفّذ تلقائيًا ما يمكن تنفيذه بأمان؛ أوقف فقط عند اعتماد/سر/قرار مالي مطلوب من المستخدم", completed: ["core_health", "github_repository", "vercel_deployment", "readiness_dashboard", "financial_approval_gate", "safe_internal_execution", "audit_log", "connector_automation_matrix", "real_connector_verification", "autonomous_cycle", "business_domain_layer", "business_api", "whatsapp_webhook_contract", "whatsapp_signature_validation", "whatsapp_outbound_api"], next: r.blockers.map((b, index) => ({ step: index + 1, ...b })), user_action_required: r.blockers.filter((b) => b.owner === "user").map((b) => b.id), financial_actions_blocked: true }; }
function systemSelfTest() { const checks = [{ id: "express", ok: Boolean(app && typeof app.get === "function") }, { id: "financial_gate", ok: isFinanciallySensitive("دفع 100 جنيه") && !isFinanciallySensitive("إنشاء منشور") }, { id: "readiness", ok: readiness().core === "healthy" }, { id: "job_store", ok: Array.isArray(state.jobs) }, { id: "approval_store", ok: Array.isArray(state.approvals) }, { id: "audit_store", ok: Array.isArray(state.audit) }, { id: "business_store", ok: Array.isArray(state.business.leads) && Array.isArray(state.business.orders) }, { id: "business_module", ok: typeof business.businessSummary === "function" }, { id: "persistence_config", ok: !persistence.enabled || Boolean(KV_REST_API_URL || DATABASE_URL) }, { id: "connector_verification_engine", ok: typeof verifyConnector === "function" }, { id: "whatsapp_webhook_contract", ok: typeof business.normalizeWhatsAppEvent === "function" && typeof business.upsertLeadFromWhatsApp === "function" }, { id: "whatsapp_signature_engine", ok: typeof isValidWhatsAppSignature === "function" }, { id: "whatsapp_outbound_engine", ok: typeof sendWhatsAppText === "function" }, { id: "autonomous_cycle", ok: typeof runAutonomousCycle === "function" }]; return { ok: checks.every((x) => x.ok), checks, testedAt: new Date().toISOString() }; }
async function executeSafeJob(job) { if (!job) return { executed: false, reason: "job_not_found" }; const sensitive = Boolean(job.financial || isFinanciallySensitive(`${job.title} ${job.description}`)); if (sensitive) { job.financial = true; if (job.status !== "approved") { job.status = "approval_required"; audit("financial_execution_blocked", { jobId: job.id, reason: "human_approval_required" }); return { executed: false, reason: "financial_approval_required" }; } job.status = "financial_execution_authorized"; job.execution = { mode: "human_approved", result: "authorization_recorded_only", externalFinancialExecution: false, note: "الموافقة البشرية سُجلت، لكن النظام لا ينفذ حركة مالية فعلية دون موصل مالي صريح وموافقة تشغيلية مناسبة." }; job.executedAt = null; audit("financial_action_authorized_not_executed", { jobId: job.id }); return { executed: false, authorized: true, reason: "financial_execution_not_configured" }; } if (job.status !== "pending") return { executed: false, reason: "not_runnable" }; job.status = "executed"; job.executedAt = new Date().toISOString(); job.execution = { mode: "safe_internal", result: "completed_without_external_financial_action" }; audit("job_auto_executed", { jobId: job.id, title: job.title }); return { executed: true, jobId: job.id }; }
async function runAutonomousCycle() { const startedAt = new Date().toISOString(); audit("autonomy_cycle_started", { startedAt }); const selfTest = systemSelfTest(); if (!selfTest.ok) { audit("autonomy_cycle_blocked", { reason: "self_test_failed" }); return { ok: false, startedAt, selfTest, executed: [], blockers: [{ owner: "system", action: "repair_self_test" }] }; } const verificationIds = Object.keys(state.connectors).filter((id) => !["github", "vercel"].includes(id) && ["configured_unverified", "error"].includes(state.connectors[id].status)); const verification = []; for (const id of verificationIds) verification.push(await verifyConnector(id)); const executed = []; const skipped = []; for (const job of state.jobs) { if (!["pending", "approved"].includes(job.status)) continue; const result = await executeSafeJob(job); if (result.executed) executed.push(result); else skipped.push({ jobId: job.id, reason: result.reason, authorized: Boolean(result.authorized) }); } const r = readiness(); audit("autonomy_cycle_completed", { executed: executed.length, skipped: skipped.length, blockers: r.blockers.length }); await saveState(); return { ok: true, startedAt, completedAt: new Date().toISOString(), selfTest, verification, executed, skipped, readiness: r }; }
function createApproval(job) { const approval = { id: `approval_${Date.now()}_${Math.random().toString(36).slice(2, 8)}`, jobId: job.id, type: "financial", status: "pending", requestedAt: new Date().toISOString() }; state.approvals.push(approval); return approval; }

app.get("/health", (req, res) => res.json({ ok: true, service: ZOZ_NAME, status: "healthy", persistence: persistence.enabled ? "enabled" : "memory_only", time: new Date().toISOString() }));
app.get("/api/state", (req, res) => res.json({ ...state, persistence: { enabled: persistence.enabled, provider: persistence.provider } }));
app.get("/api/readiness", (req, res) => res.json(readiness()));
app.get("/api/plan", (req, res) => res.json(executionPlan()));
app.get("/api/self-test", (req, res) => res.json(systemSelfTest()));
app.get("/api/audit", (req, res) => res.json({ events: state.audit.slice(-100) }));
app.get("/api/jobs", (req, res) => res.json(state.jobs));
app.get("/api/approvals", (req, res) => res.json(state.approvals));
app.get("/api/business/summary", (req, res) => res.json(business.businessSummary(state.business)));
app.get("/api/business/customers", (req, res) => res.json(state.business.customers));
app.get("/api/business/leads", (req, res) => res.json(state.business.leads));
app.get("/api/business/leads", (req, res) => res.json(state.business.leads));
app.get("/api/business/orders", (req, res) => res.json(state.business.orders));
app.get("/api/business/opportunities", (req, res) => res.json(state.business.opportunities));
app.get("/api/business/products", (req, res) => res.json(state.business.products));
app.get("/api/business/suppliers", (req, res) => res.json(state.business.suppliers));
app.get("/api/business/expenses", (req, res) => res.json(state.business.expenses));
app.post("/api/business/leads", async (req, res) => { try { const lead = business.addLead(state.business, req.body); audit("business_lead_created", { leadId: lead.id }); await saveState(); res.status(201).json(lead); } catch (error) { res.status(400).json({ error: error.message }); } });
app.post("/api/business/leads/:id/stage", async (req, res) => { try { const lead = business.moveLead(state.business, req.params.id, req.body.stage); audit("business_lead_stage_changed", { leadId: lead.id, stage: lead.stage }); await saveState(); res.json(lead); } catch (error) { res.status(400).json({ error: error.message }); } });
app.post("/api/business/orders", async (req, res) => { try { const order = business.createOrder(state.business, req.body); audit("business_order_created", { orderId: order.id, financialApprovalRequired: true }); await saveState(); res.status(201).json(order); } catch (error) { res.status(400).json({ error: error.message }); } });
app.post("/api/business/opportunities", async (req, res) => { try { const opportunity = business.registerOpportunity(state.business, req.body); audit("business_opportunity_registered", { opportunityId: opportunity.id, score: opportunity.score }); await saveState(); res.status(201).json(opportunity); } catch (error) { res.status(400).json({ error: error.message }); } });
app.post("/api/business/actions/plan", (req, res) => res.json(business.planAction(req.body.action, req.body.payload)));
app.post("/api/jobs", async (req, res) => { const { title, description = "" } = req.body; if (!title) return res.status(400).json({ error: "عنوان المهمة مطلوب" }); const financial = isFinanciallySensitive(`${title} ${description}`); const job = { id: Date.now().toString(), title, description, financial, status: financial ? "approval_required" : "pending", createdAt: new Date().toISOString() }; state.jobs.push(job); if (financial) createApproval(job); audit("job_created", { jobId: job.id, financial, status: job.status }); await saveState(); res.status(201).json(job); });
app.post("/api/jobs/:id/approve", async (req, res) => { const job = state.jobs.find((x) => x.id === req.params.id); if (!job) return res.status(404).json({ error: "المهمة غير موجودة" }); job.status = "approved"; job.approvedAt = new Date().toISOString(); if (job.financial) { const approval = state.approvals.find((item) => item.jobId === job.id && item.status === "pending"); if (approval) { approval.status = "approved"; approval.approvedAt = job.approvedAt; } else { const created = createApproval(job); created.status = "approved"; created.approvedAt = job.approvedAt; } } audit("job_approved", { jobId: job.id, financial: job.financial }); await saveState(); res.json(job); });
app.post("/api/jobs/:id/reject", async (req, res) => { const job = state.jobs.find((x) => x.id === req.params.id); if (!job) return res.status(404).json({ error: "المهمة غير موجودة" }); job.status = "rejected"; job.rejectedAt = new Date().toISOString(); const approval = state.approvals.find((item) => item.jobId === job.id && item.status === "pending"); if (approval) { approval.status = "rejected"; approval.rejectedAt = job.rejectedAt; } audit("job_rejected", { jobId: job.id, financial: job.financial }); await saveState(); res.json(job); });
app.post("/api/jobs/:id/execute", async (req, res) => { const job = state.jobs.find((x) => x.id === req.params.id); if (!job) return res.status(404).json({ error: "المهمة غير موجودة" }); const result = await executeSafeJob(job); if (result.reason === "financial_approval_required") { await saveState(); return res.status(403).json({ error: "موافقة المستخدم مطلوبة قبل أي إجراء مالي", status: "approval_required" }); } if (result.reason === "financial_execution_not_configured") { await saveState(); return res.status(202).json({ ok: true, job, status: job.status, note: "تم تسجيل الموافقة البشرية فقط؛ لم يتم تنفيذ أي حركة مالية فعلية." }); } if (!result.executed) return res.status(409).json({ error: "المهمة ليست قابلة للتنفيذ", status: job.status }); await saveState(); res.json({ ok: true, job, note: "تم تنفيذ المهمة الداخلية الآمنة فقط؛ لا يوجد إجراء مالي أو خارجي تلقائي هنا." }); });
app.post("/api/replies/preview", (req, res) => { const { text = "" } = req.body; res.json({ text, financialApprovalRequired: isFinanciallySensitive(text), ready: true }); });
app.get("/api/connectors/status", (req, res) => { const values = Object.values(state.connectors); res.json({ ...state.connectors, summary: { connected: values.filter((x) => x.status === "verified" || x.status === "connected").length, deployed: values.filter((x) => x.status === "deployed").length, configured: values.filter((x) => x.status === "configured_unverified").length, needsSetup: values.filter((x) => x.status === "needs_setup").length, errors: values.filter((x) => x.status === "error").length }, persistence: { enabled: persistence.enabled, provider: persistence.provider } }); });
app.get("/api/connectors/requirements", (req, res) => res.json({ github: "متصل عبر GitHub Connector", vercel: "النظام منشور على Vercel", database: "DATABASE_URL أو KV_REST_API_URL + KV_REST_API_TOKEN", whatsapp: "WHATSAPP_ACCESS_TOKEN + WHATSAPP_PHONE_NUMBER_ID + WHATSAPP_VERIFY_TOKEN + WHATSAPP_APP_SECRET (موصى به للتحقق من التوقيع)", youtube: "YOUTUBE_ACCESS_TOKEN عبر OAuth 2.0", tiktok: "TIKTOK_ACCESS_TOKEN عبر OAuth 2.0", linkedin: "LINKEDIN_ACCESS_TOKEN عبر OAuth 2.0", shopify: "SHOPIFY_ACCESS_TOKEN + SHOPIFY_STORE_DOMAIN", financialRule: "لا يتم الدفع أو الشراء أو التحويل أو استلام الأموال دون موافقة المستخدم، ولا توجد حركة مالية فعلية مدمجة في النواة." }));
app.get("/api/connectors/verify", async (req, res) => { const requested = req.query.id ? String(req.query.id).split(",").filter(Boolean) : Object.keys(state.connectors); const results = []; for (const id of requested) results.push(await verifyConnector(id)); await saveState(); res.json({ ok: results.every((x) => x.verified), results, readiness: readiness() }); });
app.get("/api/whatsapp/webhook", (req, res) => { const mode = req.query["hub.mode"]; const token = req.query["hub.verify_token"]; const challenge = req.query["hub.challenge"]; if (!process.env.WHATSAPP_VERIFY_TOKEN) return res.status(503).send("WHATSAPP_VERIFY_TOKEN not configured"); if (mode === "subscribe" && token === process.env.WHATSAPP_VERIFY_TOKEN) { audit("whatsapp_webhook_verified", {}); return res.status(200).send(challenge || ""); } audit("whatsapp_webhook_verification_failed", { mode: mode || null }); return res.sendStatus(403); });
app.post("/api/whatsapp/webhook", async (req, res) => { if (!isValidWhatsAppSignature(req)) { audit("whatsapp_webhook_signature_rejected", {}); return res.sendStatus(401); } const body = req.body || {}; const events = business.normalizeWhatsAppEvent(body); let processed = 0; for (const event of events) { if (!event.from) continue; const result = business.upsertLeadFromWhatsApp(state.business, { phone: event.from, name: event.name, request: event.text }); audit("whatsapp_lead_upserted", { messageId: event.messageId, customerId: result.customer.id, leadId: result.lead.id, created: result.created, type: event.type }); processed += 1; } audit("whatsapp_webhook_received", { object: body.object || null, entries: Array.isArray(body.entry) ? body.entry.length : 0, events: events.length, processed }); await saveState(); res.status(200).json({ ok: true, received: events.length, processed }); });
app.post("/api/whatsapp/send", async (req, res) => { const { to, text } = req.body || {}; if (!to || !text) return res.status(400).json({ ok: false, error: "recipient_and_text_required" }); if (isFinanciallySensitive(text)) { audit("whatsapp_outbound_blocked", { reason: "financial_approval_required" }); return res.status(403).json({ ok: false, error: "financial_approval_required" }); } try { const result = await sendWhatsAppText(to, text); if (!result.ok) return res.status(result.status || 502).json(result); return res.status(200).json(result); } catch (error) { audit("whatsapp_outbound_error", { message: error.message }); return res.status(502).json({ ok: false, error: "whatsapp_send_failed" }); } });
app.get("/api/automation/status", (req, res) => { const connectors = Object.entries(state.connectors).map(([id, connector]) => ({ id, label: connector.label, status: connector.status, automation: connector.automation, externallyExecutable: connector.status === "verified" && connector.automation === "ready" })); res.json({ mode: "safe_autonomy", financialApprovalRequired: true, externalActionsAllowedOnlyForVerifiedConnectors: true, financialExternalExecution: false, autonomousCycleEndpoint: "/api/automation/cycle", connectors, selfTest: systemSelfTest(), business: business.businessSummary(state.business), approvalsPending: state.approvals.filter((item) => item.status === "pending").length }); });
app.get("/api/automation/cycle", async (req, res) => { const denied = authorizeCron(req, res); if (denied) return; try { res.json(await runAutonomousCycle()); } catch (error) { audit("autonomy_cycle_error", { message: error.message }); await saveState(); res.status(500).json({ ok: false, error: "autonomy_cycle_failed" }); } });
app.post("/api/automation/cycle", async (req, res) => { const denied = authorizeCron(req, res); if (denied) return; try { res.json(await runAutonomousCycle()); } catch (error) { audit("autonomy_cycle_error", { message: error.message }); await saveState(); res.status(500).json({ ok: false, error: "autonomy_cycle_failed" }); } });

app.get("/{*splat}", (req, res) => res.sendFile(path.join(__dirname, "public", "index.html")));

loadState().then(() => { audit("system_started", { persistence: persistence.enabled, persistenceProvider: persistence.provider, autonomousCycle: true, businessLayer: true, whatsappWebhook: true, whatsappSignatureValidation: Boolean(process.env.WHATSAPP_APP_SECRET), whatsappOutbound: true, financialExternalExecution: false }); }).catch((error) => { audit("system_start_error", { message: error.message }); });

module.exports = app;

if (!process.env.VERCEL) {
  app.listen(PORT, () => console.log(`${ZOZ_NAME} running on port ${PORT}`));
}
