// Provider-neutral video contract. Heavy inference is deliberately delegated to an attached ZOZ GPU renderer.
const URL_BASE=String(process.env.ZOZ_NATIVE_VIDEO_RENDERER_URL||"").replace(/\/$/,"");
async function renderVideo(input={}){if(!input.prompt&&!input.description)return {ok:false,status:"blocked",reason:"video_prompt_required"}; if(!URL_BASE)return {ok:false,status:"waiting_for_renderer",reason:"zoz_native_video_renderer_not_available",next:"Set ZOZ_NATIVE_VIDEO_RENDERER_URL when a GPU video worker is available."}; try{const r=await fetch(URL_BASE+"/v1/videos/generations",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(input)}); const t=await r.text(); let b;try{b=t?JSON.parse(t):{};}catch{b={text:t.slice(0,500)}} return r.ok?{...b,ok:true,executionMode:"zoz-native-video-renderer"}:{ok:false,status:"failed",reason:b.detail||b.error||"native_video_renderer_failed",httpStatus:r.status};}catch(e){return {ok:false,status:"unavailable",reason:"native_video_renderer_unreachable",detail:e.message}}}
module.exports={renderVideo};